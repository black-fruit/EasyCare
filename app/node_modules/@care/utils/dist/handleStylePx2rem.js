"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var px2rem_1 = __importDefault(require("./px2rem"));
/**
 * @description px的样式转换为rem的样式。注意！如果是 px ，务必加上 px 单位
 * @param originStyle
 *
 * 把 style 里面的 px 值转成 rem 值，如
 * {
 *   fontSize: '32px',
 *   width: '100px',
 * }
 *
 * 转成
 *
 * {
 *   fontSize: '0.5rem',
 *   width: '1.6rem'
 * }
 */
function handleStylePx2rem(originStyle, remUnit) {
    if (remUnit === void 0) { remUnit = 75; }
    var styles = {};
    // px转rem处理
    for (var key in originStyle) {
        if (originStyle.hasOwnProperty(key)) {
            var value = originStyle[key].toString();
            styles[key] = value.replace(/[1-9]\d*px/gi, function (px) {
                return px2rem_1.default(parseInt(px), remUnit);
            });
        }
    }
    return styles;
}
;
exports.default = handleStylePx2rem;
