"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * @description 比较字符串版本号
 * @param str1 版本号
 * @param str2 版本号
 * @param split 分隔符，默认是.
 * @returns 1 | -1 | 0
 */
function compareStrings(str1, str2, split) {
    if (split === void 0) { split = '.'; }
    var arr1 = str1.split(split);
    var arr2 = str2.split(split);
    var len = Math.max(arr1.length, arr2.length);
    while (arr1.length < len) {
        arr1.push('0');
    }
    while (arr2.length < len) {
        arr2.push('0');
    }
    for (var i = 0; i < len; i++) {
        var num1 = parseInt(arr1[i]);
        var num2 = parseInt(arr2[i]);
        if (num1 > num2) {
            return 1;
        }
        else if (num1 < num2) {
            return -1;
        }
    }
    return 0;
}
exports.default = compareStrings;
