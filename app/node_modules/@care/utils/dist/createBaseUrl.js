"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * @description 根据当前的location信息创建一个新的url
 * @param {string} appendUrl
 * @param {boolean} useSearch
 * @param {boolean} useHash
 * @returns {string}
 */
function createBaseUrl(appendUrl, useSearch, useHash, localLocation) {
    if (useSearch === void 0) { useSearch = true; }
    if (useHash === void 0) { useHash = true; }
    if (typeof window === 'undefined') {
        throw new Error('window is not defined');
    }
    localLocation = window.location;
    var protocol = localLocation.protocol, hostname = localLocation.hostname, port = localLocation.port, search = localLocation.search, hash = localLocation.hash;
    var url = protocol + "//" + hostname;
    url += port ? ":" + port : '';
    url += appendUrl;
    url += useSearch ? "" + search : '';
    url += useHash ? "" + hash : '';
    return url;
}
exports.default = createBaseUrl;
