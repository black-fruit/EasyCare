/**
* @jest-environment node
*/

import downloadFile from '../../download-file';
describe('node环境，测试 downloadFile 函数', () => {
  it('window不存在，抛出错误', () => {
    // @ts-ignore
    expect(() => downloadFile()).toThrow();  
  })
})