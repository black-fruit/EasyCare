/**
* @jest-environment node
*/

import copyTextToClipBoard from '../../copyTextToClipBoard';
describe('node环境，测试 copyTextToClipBoard 函数', () => {
  it('window不存在，抛出错误', () => {
    expect(() => copyTextToClipBoard('hello')).toThrow();  
  })
})