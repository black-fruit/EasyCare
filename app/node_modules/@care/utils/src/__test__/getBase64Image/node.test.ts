/**
* @jest-environment node
*/

import getBase64Image from '../../getBase64Image';
describe('node环境，测试 getBase64Image 函数', () => {
  it('window不存在，抛出错误', () => {
    // @ts-ignore
    expect(() => getBase64Image()).toThrow();  
  })
})