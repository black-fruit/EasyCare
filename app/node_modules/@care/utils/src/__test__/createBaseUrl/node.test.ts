/**
* @jest-environment node
*/

import createBaseUrl from '../../createBaseUrl';
describe('node环境，测试 createBaseUrl 函数', () => {
  it('window不存在，抛出错误', () => {
    expect(() => createBaseUrl('/hello', false, false)).toThrow();  
  })
})