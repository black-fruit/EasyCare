/**
* @jest-environment node
*/

import getImage from '../../getImage';
describe('node环境，测试 getImage 环境', () => {
  it('window不存在，抛出错误', () => {
    expect(() => getImage('success')).toThrow();  
  })
})
