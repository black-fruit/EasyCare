/**
* @jest-environment node
*/

import makePageResumeToBottomWhenInputBlur from '../../makePageResumeToBottomWhenInputBlur';
describe('node环境，测试 copyText 函数', () => {
  it('window不存在，抛出错误', () => {
    expect(() => makePageResumeToBottomWhenInputBlur()).toThrow();  
  })
})