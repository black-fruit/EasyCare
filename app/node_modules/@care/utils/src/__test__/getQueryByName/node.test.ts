/**
* @jest-environment node
*/

import getQueryByName from '../../getQueryByName';
describe('node环境，测试 getQueryByName 函数', () => {
  it('window不存在，抛出错误', () => {
    expect(() => getQueryByName('name')).toThrow();  
  })
})