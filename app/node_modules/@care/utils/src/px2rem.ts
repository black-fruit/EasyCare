/**
 * 获取px转化为对应的rem值
 * @param pxNum px值
 * @return {string} px对应的rem值
 */
function px2rem(pxNum: number, remUnit: number = 75): string {
  const result = pxNum / remUnit;

  return result + 'rem';
}

export default px2rem;