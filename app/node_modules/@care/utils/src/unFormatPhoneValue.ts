/**
 * 去格式化手机号码
 * 如 133-4567-8888 会变成 13345678888
 */
const unFormatPhoneValue = (value: string) => {
 if (typeof value !== 'string') {
   return '';
 }
 return value.trim().replace(/[^\d]/g, '');
}

export default unFormatPhoneValue;