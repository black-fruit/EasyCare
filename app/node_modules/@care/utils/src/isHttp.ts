const httpReg = /^(https?):\/\//;

/**
 * 字符串是否为 http 或者 https 的链接
 * @param {string} url 
 */
function isHttp(url?: string | null) {
  if (!url) {
    return false;
  }
  return httpReg.test(url);
}

export default isHttp;