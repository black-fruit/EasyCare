import childNodeRemovePolyfill from './polyfill/ChildNode.remove';
// 适配小程序
if (typeof window !== 'undefined' && typeof Element !== 'undefined' && typeof CharacterData !== 'undefined' && typeof DocumentType !== 'undefined') {
    childNodeRemovePolyfill([Element.prototype, CharacterData.prototype, DocumentType.prototype]);
}
/**
 * 复制文本到粘贴板
 * @param {string} text 要被复制的字符串
 */
var copyText = function (text) {
    if (typeof window === 'undefined') {
        throw new Error('window is undefined');
    }
    var input = document.createElement('input');
    input.type = 'text';
    input.value = text;
    document.body.appendChild(input);
    input.select();
    var copyResult = document.execCommand('copy');
    input.remove();
    return copyResult;
};
export default copyText;
